[build]
  command = "corepack enable && cd studio && pnpm install --frozen-lockfile && pnpm run sync:all && cd .. && pnpm install --frozen-lockfile && pnpm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"
